# SCS-RG TeamClover — Multi-Hazard Smart Campus Safety & Response Grid

## Quick Start

### Backend
```bash
cd backend
docker-compose up -d
npx prisma migrate deploy
npm run dev
```

### Frontend
```bash
cd frontend
npm install
npm run dev
```

### Firmware (Wokwi)
See `firmware/README.md` for setup instructions.
