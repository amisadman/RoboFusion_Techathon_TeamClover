# Point at the deployed backend for normal dev. Switch to
# http://localhost:4000 only if your backend teammate is running it
# locally and you specifically need to test against local changes that
# aren't deployed yet.
NEXT_PUBLIC_BACKEND_URL=https://robofusion-techathon-teamclover.onrender.com
